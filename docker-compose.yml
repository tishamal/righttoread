version: '3.8'

services:
  # React Frontend Service
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: right-to-read-frontend
    ports:
      - "80:80"
    restart: always
